class Entry {
  constructor(id, createdOn, title, description, userId) {
    this.id = id;
    this.createdOn = createdOn;
    this.title = title;
    this.description = description;
    this.userId = userId;
  }
}

export default Entry;
